= link_to board_task_path(@board,task.id) do
  .card
    - if task.eyecatch.attached?
      .card_image
        = image_tag task.eyecatch
    .card_content
      .card_title
        = task.title
      .card_detail
        = task.content
